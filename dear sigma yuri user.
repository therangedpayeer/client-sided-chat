-- dear sigma yuri user this is not a fe chat script you may take this if you would like for your own script

local player = game.Players.LocalPlayer
local chatService = game:GetService("Chat")

-- Message content
local message = [[
Welcome to Sigma Yuri
Current Version 3.5
   Free/Paid
press f9 or type /console to see updates 
  FREE version

only you can see this!
]]

-- Function to display the message in chat
local function displayMessage()
    chatService:Chat(player.Character.Head, message, Enum.ChatColor.Red)
end

-- Send the message when the game starts
displayMessage()
